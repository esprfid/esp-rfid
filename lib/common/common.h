#ifndef __COMMON_H__
#define __COMMON_H__

#define ARRAY_ITEMS(arr)        (sizeof(arr) / sizeof(*arr))

#define CONF_FILE_PATH          "/config.json"
#define EVENTLOG_FILE_PATH      "/eventlog.json"
#define LATESTLOG_FILE_PATH     "/latestlog.json"

#endif